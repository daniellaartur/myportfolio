var cardDisplay = document.getElementById('showmodel');
var showBtn = document.getElementById('showBtn');

showBtn.onclick = function(){
    //when clicking a button, the window should display the model

    cardDisplay.style.display = "block";
}